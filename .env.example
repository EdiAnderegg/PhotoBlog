# Django
DJANGO_SECRET_KEY=changeme
DJANGO_DEBUG=True
DJANGO_ALLOWED_HOSTS=localhost,127.0.0.1

# PostgreSQL
# POSTGRES_HOST=db matches the Docker Compose service name.
# Override to localhost if running Django outside of Docker Compose.
POSTGRES_DB=photoblog
POSTGRES_USER=photoblog
POSTGRES_PASSWORD=changeme
POSTGRES_HOST=db
POSTGRES_PORT=5432
